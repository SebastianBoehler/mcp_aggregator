{
  "name": "mcp-aggregator",
  "version": "0.1.0",
  "description": "Aggregates multiple MCP servers and exposes them from a single host instance.",
  "license": "MIT",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@langchain/openai": "^0.5.16",
    "@modelcontextprotocol/sdk": "^1.13.2",
    "dotenv": "^17.0.1",
    "fs": "^0.0.1-security",
    "langchain": "^0.3.29",
    "mcp-use": "^0.1.3"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.2.17"
  }
}
